library (vegan)
vegdist


plot_pi=function(x, plot=FALSE, Method= "co2" ) 
{
  if (anyNA (x)==TRUE){stop ("NA values") }
  Met  <- c("")
  M.x <- as.matrix(x)
  if 
  
  mutate(H=hour(pi_d3$Hora),
         D=day(pi_d3$Data),
         M= month(pi_d3$Data),
         Y= year(pi_d3$Data))  
    
}
 ?pmatch()?pmatch